"""Portcullis MCP gateway package."""

# Canonical release version.  Bumped in lockstep with the alembic migration
# series (v0.8 == migration 0013) and reported by the opt-in telemetry
# heartbeat so the operator can see the version distribution of self-hosts.
__version__ = "0.8.0"
